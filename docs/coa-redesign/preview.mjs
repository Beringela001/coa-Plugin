import {records,currentDestination} from './evidence.mjs';
const key=new URLSearchParams(location.search).get('batch')==='current'?'current':'past';
const r=records[key], destination=currentDestination(r);
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const rows=r.rows.map(([name,result,status,detail])=>`<details class="result"><summary><span><strong>${esc(name)}</strong><span class="resultvalue">${esc(result)}</span></span><span class="resultend"><span class="badge ${status==='Pass'?'pass':'neutral'}">${status==='Pass'?'Lab: Pass':'Value reported'}</span><span aria-hidden="true">+</span></span></summary><div class="resultdetail">${esc(detail)} <a href="${r.pdf}" target="_blank" rel="noopener">Read original certificate ↗</a></div></details>`).join('');
document.querySelector('#content').innerHTML=`
<nav class="crumb" aria-label="Breadcrumb"><a href="https://pepselect.com/testing/">Quality archive</a><span>/</span><a href="https://pepselect.com/testing/retatrutide-30mg/">Retatrutide 30 mg</a><span>/</span><span>${r.batch}</span></nav>
<div class="pageheading"><div><p class="eyebrow">BATCH LABORATORY RECORD</p><h1>Retatrutide <span>30 mg</span></h1><p class="batchcode">Batch <strong>${r.batch}</strong></p></div><span class="badge state ${key==='past'?'past':'pass'}">${r.state} batch</span></div>
<section class="route ${key}" aria-label="Batch status and next step"><div><strong>${key==='past'?'This is a historical batch.':'This is the current documented batch.'}</strong><p>${key==='past'?'Keep this report if its batch code matches your vial. Today’s product has a separate batch and report.':'Match RT3026233GX to your vial. Check the product page for current availability and pricing.'}</p></div><a class="button" data-coa-action="${key==='past'?'current_report':'product'}" href="${key==='past'?'?batch=current':destination.product}">${key==='past'?'View current batch':'View matching product'} <span aria-hidden="true">→</span></a></section>
<div class="columns"><div class="reading"><section class="overview"><p class="eyebrow">AT A GLANCE</p><h2>What this report tells you</h2><p>The lab reports identity, purity and quantity for this batch. Each test’s wording and result remain separate below.</p><div class="metrics"><div><span>Peptide purity</span><strong>${r.purity}</strong><small>Purity is not the amount in the vial.</small></div><div><span>${key==='past'?'Net peptide content':'Net content'}</span><strong>${r.content}</strong><small>Quantity reported by the lab.</small></div><div><span>Labeled strength</span><strong>30 mg</strong><small>Product label, not a lab result.</small></div></div><p class="release"><span class="dot"></span><strong>Pep Select release record: Approved</strong><span>Individual laboratory results are listed below. “Reported” does not mean “Pass.”</span></p></section>
<p class="quicksource"><a href="${r.pdf}" target="_blank" rel="noopener">Original PDF ↗</a> · <a href="${r.source}" target="_blank" rel="noopener">Laboratory source ↗</a></p><section class="tests"><div class="sectionhead"><div><p class="eyebrow">LABORATORY FINDINGS</p><h2>Read the results</h2></div><span>7 categories</span></div><p class="helper">Open a result for its method, units and limits.</p>${rows}<p class="helper">Results apply to the submitted sample(s). They do not certify another batch or imply human-use suitability.</p></section>
<details class="reference"><summary>Report dates, batch details & source notes</summary><dl><dt>Laboratory</dt><dd>${r.lab}</dd><dt>Reference</dt><dd>${r.reference}</dd><dt>${r.dateLabel}</dt><dd>${r.date}</dd><dt>Vial cap / crimp</dt><dd>${r.cap} / ${r.crimp}</dd><dt>Original PDF</dt><dd>${r.pages} page${r.pages===1?'':'s'}</dd></dl><p>${r.notes}</p></details>
<details class="reviewnotes"><summary>Review only: differences from today’s page</summary><p>These proposed source-based labels need approval. Saved records have not been changed.</p><ul>${r.discrepancies.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><p><a href="${r.url}" target="_blank" rel="noopener">Compare the live record ↗</a></p></details>
</div><aside><section class="sourcecard"><p class="eyebrow">MATCH YOUR VIAL</p><img src="${r.photo}" alt="Batch ${r.batch} vial"/><strong class="photobatch">${r.batch}</strong><p>Match the batch code first. Cap color alone does not identify a batch.</p><hr><p class="eyebrow">ORIGINAL LABORATORY SOURCE</p><h3>${r.lab}</h3><p>${r.dateLabel}<br><strong>${r.date}</strong></p><a class="button outline" href="${r.source}" target="_blank" rel="noopener" data-coa-action="laboratory">Open lab report ↗</a><a class="textlink" href="${r.pdf}" target="_blank" rel="noopener" data-coa-action="pdf">Download original PDF · ${r.pages} page${r.pages===1?'':'s'}</a></section><section class="help"><h3>New to COAs?</h3><p>A Certificate of Analysis records the laboratory’s findings for a specific batch.</p><a href="https://pepselect.com/guides/how-to-review-research-peptide-documentation/">Read the existing COA guide →</a></section></aside></div>
<section class="next"><div><p class="eyebrow">CONTINUE WITH THE RIGHT BATCH</p><h2>${key==='past'?'Looking for today’s product?':'Ready to view the product?'}</h2><p>Current documented batch: <strong>${destination.batch}</strong>. Each batch has its own report.</p></div><div><a class="button" href="${destination.product}" data-coa-action="product">View Retatrutide 30 mg product →</a><a class="textlink" href="https://pepselect.com/testing/retatrutide-30mg/">View all batch reports</a></div></section>`;
document.querySelectorAll('.reviewbar a').forEach(a=>{if(a.getAttribute('href')===`?batch=${key}`)a.setAttribute('aria-current','page');});
